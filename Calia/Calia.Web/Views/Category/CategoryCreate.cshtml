@model CategoryDto

<form asp-action="CategoryCreate">
    <div class="container">
        <div class="d-flex justify-content-center mt-4">
            <div class="card" style="width: 100%; max-width: 600px;">
                <div class="card-header">
                    <h6 class="card-title mb-0 text-center">Create Category</h6>
                </div>
                <div class="card-body">
                    <div class="row gy-3">
                        <div class="col-12">
                            <label class="control-label" style="font-size: 20px; color: #343a40;">Name</label>
                            <input asp-for="Name" class="form-control" placeholder="Enter category name" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="col-12">
                            <label class="control-label" style="font-size: 20px; color: #343a40;">Display Order</label>
                            <input asp-for="DisplayOrder" class="form-control" placeholder="Enter display order" type="number" />
                            <span asp-validation-for="DisplayOrder" class="text-danger"></span>
                        </div>

                        <div class="col-12 form-switch switch-warning d-flex align-items-center mb-3">
                            <input asp-for="ProductCount" class="form-check-input" type="checkbox" id="flexSwitchCheckDefault">
                            <label class="form-check-label fw-medium text-secondary" for="flexSwitchCheckDefault">Ürün sayısı var mı?</label>
                        </div>

                        <div class="text-center mt-3">
                            <button type="submit" class="btn btn-success">Create</button>
                        </div>

                        <div class="col-12 text-center mt-2">
                            <a asp-action="CategoryIndex" class="btn btn-outline-secondary">Back to List</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
